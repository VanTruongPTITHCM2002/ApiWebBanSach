// export class FilterParser {
//   static parse(filter: string) {
//     if (!filter) return [];

//     const tokens = filter.replace(/\s+/g, '').split(/ (and|or) /i);

//     const conditions = [];

//     for (let i = 0; i < tokens.length === 'and' || )
//   }
// }
